============
Installation
============

At the command line::

    $ easy_install django-newsletter2go-v2

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-newsletter2go-v2
    $ pip install django-newsletter2go-v2
